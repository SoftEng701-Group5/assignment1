/* Default timer set to 25 minutes (1500 seconds) */
export const defaultWorkTimer = {
  seconds: 1500,
};

/* Default break timer set to 5 minutes (300 seconds) */
export const defaultBreakTimer = {
  seconds: 300,
};

export const defaultPlay = false;
export const defaultTimerModalShow = false;
