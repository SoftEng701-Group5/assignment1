export default function TimerResize({resizeStyle, arrow, line}) {

    return (
        <button className={resizeStyle}>
            <div>
                <div className={arrow}>

                </div>
            </div>
        </button>

    );

}